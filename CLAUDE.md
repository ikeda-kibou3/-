# etax-auto — Claude Code 向けの前提

税理士事務所の毎月のルーチン業務（決算通知書の取得とチェックリスト作成）を
自動化するツールです。利用者は税理士事務所の職員で、プログラマではありません。

## 大前提

- **関与先の実データを外部に送らない。** `data/clients.csv`、`data/credentials.enc`、
  `out/`、`logs/` の中身をWebやAPIに投げない。要約もしない。
- **暗証番号を平文で書き出す機能を追加しない。** 復号は実行時メモリ内だけ。
- **e-Tax に総当たりや並列アクセスをする実装にしない。** 1社ずつ、間隔を空けて。
  `settings.toml` の `interval_sec` を0にする提案をしない。

## 画面まわりの直し方

e-Tax の画面が変わったときは **`config/selectors.yaml` だけ** を直す。
`src/etax_auto/etax.py` のロジックは触らない。

- 候補は上から順に試される。新しい言い回しは**一番上**に足す。古いものは消さない。
- `{text: ...}` `{label: ...}` を優先。`{css: ...}` は最後の手段。
- 失敗時の画面は `logs/capture/*.png` と `*.html` に残る。ファイル名の
  `miss_<ステップ名>` が直すべきキー。

## 変更するときの確認

```
PYTHONPATH=src python -m pytest tests -q
```

`tests/mock_etax/` は e-Tax を模した静的HTML。ブラウザ操作を変えたら
ここも合わせて更新し、`test_etax_flow.py` が通ることを確認する。

決算期の判定（`src/etax_auto/clients.py`）を変えたときは、
延長特例・免税事業者・年またぎのテストが通ることを必ず確認する。
ここを間違えると「処理すべき関与先が漏れる」という業務事故になる。

## 書き方

- ユーザー向けの文字列・コメント・ドキュメントは**日本語**。
- エラーメッセージは「何が起きたか」と「次に何をすればよいか」を書く。
  例: 「暗証番号が未登録です。`etax-auto cred set <識別番号>` で登録してください。」
- 例外を握りつぶさない。ただし1社の失敗で全社が止まらないよう、
  社ごとに捕まえて `_結果一覧.csv` の「メモ」に理由を残す。

## やらないこと

- 申告書の作成・送信は範囲外。このツールは**お知らせの取得と印刷物の準備**まで。
- 中身の判断は税理士が行う。自動で正誤を判定する機能は入れない。
