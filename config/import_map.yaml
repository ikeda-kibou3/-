# =====================================================================
#  会計・税務ソフトのCSVを読み込むときの列名の対応
#
#  `etax-auto import-clients` が列名を自動で突き合わせます。
#  当たらなかった列は画面に出るので、ここに1行足してください。
#  ここに書いたものが、内蔵の候補より優先されます。
#
#  左が当ツールの項目、右がソフト側の列名です。
# =====================================================================

aliases:
  # code:         ["関与先コード", "顧問先コード"]
  # name:         ["法人名", "顧問先名"]
  # office:       ["事務所", "部門名"]
  # user_id:      ["利用者識別番号"]
  # fiscal_month: ["決算月", "事業年度終了日"]
  # staff:        ["担当者"]
  # consumption_taxable: ["消費税課税区分"]
